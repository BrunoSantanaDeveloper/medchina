# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

## Codex shared context

Read `../../AGENTS.md`, `../../CLAUDE.md`, this directory's `CLAUDE.md`, and
`../../.claude/rules/mobile-boundaries.md` before working in `apps/mobile`.
Load the `mobile-screen` skill before creating or substantially editing a
screen.

Do not duplicate those rules here. Update the canonical document or rule so
Claude and Codex receive the same change.
