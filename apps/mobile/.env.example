# Supabase (same project as apps/web — only the env var prefix differs).
# Without these the app stays browsable and shows a configuration hint.
EXPO_PUBLIC_SUPABASE_URL=""
EXPO_PUBLIC_SUPABASE_ANON_KEY=""
