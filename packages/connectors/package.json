{
  "name": "@flyee/connectors",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Framework for per-organization connections to external APIs: connector registry, service-role secret storage, and sync orchestration via Inngest. Ships no concrete connectors — derived projects register their own.",
  "exports": {
    ".": "./src/index.ts",
    "./jobs": "./src/jobs.ts"
  },
  "dependencies": {
    "@flyee/auth": "*",
    "@flyee/jobs": "*",
    "@supabase/supabase-js": "^2.50.0"
  }
}
